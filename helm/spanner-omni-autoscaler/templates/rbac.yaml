{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Release.Name }}-spanner-omni-autoscaler
rules:
  # Permissions to manage SpannerOmniAutoscaler CRs
  - apiGroups:
      - autoscaling.spanner.google.com
    resources:
      - spanneromniautoscalers
      - spanneromniautoscalers/status
    verbs:
      - get
      - list
      - watch
      - update
      - patch
  # Permissions to observe and patch Spanner Omni StatefulSets
  - apiGroups:
      - apps
    resources:
      - statefulsets
      - statefulsets/scale
      - statefulsets/status
    verbs:
      - get
      - list
      - watch
      - update
      - patch
  # Permissions to inspect pods and events
  - apiGroups:
      - ""
    resources:
      - pods
      - persistentvolumeclaims
      - events
    verbs:
      - get
      - list
      - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ .Release.Name }}-spanner-omni-autoscaler
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ .Release.Name }}-spanner-omni-autoscaler
subjects:
  - kind: ServiceAccount
    name: {{ .Values.serviceAccount.name }}
    namespace: {{ .Release.Namespace }}
{{- end }}
